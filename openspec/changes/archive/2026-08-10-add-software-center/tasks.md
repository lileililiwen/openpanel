## 1. TDD and Security Tests

- [x] 1.1 Unit-test catalog identifiers, semantic versions, platform constraints, component/application recipes, plan digests/tokens, dependency graphs, and every job transition before implementing them. — `crates/openpanel-domain/tests/software_center.rs` (7 tests covering `CatalogId`/`PackageId`/`SoftwareVersion`/`SupportedPlatform`, `DependencyGraph` cycle detection and `dependents_of`, `SoftwarePlan` digest stability, `ConfirmationToken` scope/TTL/one-use, `SoftwareJob` state machine + `reconcile_interrupted`).
- [x] 1.2 Property-test manifest canonicalization/signature rejection, plan digest stability, fixed package identifiers, state-machine terminality, secret serialization, and archive path/size/count safety. — `crates/openpanel-domain/tests/software_center.rs` proptest suite (`arbitrary_package_ids_never_accept_command_syntax`, `terminal_jobs_never_restart`); plus `crates/openpanel-app/tests/software_center.rs` `remote_catalog_requires_valid_signature_schema_expiry_and_strict_payload` and `verified_artifacts_extract_under_staging_and_reject_symlinks_or_tampering`.
- [x] 1.3 Service-test discovery/adoption, preview/execution TOCTOU rejection, package locking, install/update/remove, validation/rollback, interruption recovery, RBAC, cancellation checkpoints, redaction, and audit using strict mocks. — `crates/openpanel-app/tests/software_center.rs` (`discovery_reports_external_components_without_taking_ownership`, `stale_host_state_rejects_execution_before_package_mutation`, `failed_post_install_validation_rolls_back_and_never_reports_success`, `non_owner_is_rejected_before_discovery_or_commands`, `apt_adapter_uses_fixed_program_and_end_of_options_before_validated_packages`, `package_diagnostics_are_bounded_and_redact_secret_bearing_lines`, `external_component_requires_explicit_adoption_before_removal`, `cancellation_waits_for_package_checkpoint_then_rolls_back`).
- [x] 1.4 Service-test WordPress and Drupal success/failure transactions, missing PHP extensions, one-time credentials, artifact verification, safe extraction, health checks, and reverse-order cleanup through mocked public ports. — `crates/openpanel-app/tests/software_center.rs` (`failed_application_health_check_unwinds_job_created_resources`, `integrated_application_failure_removes_only_created_resources_in_reverse_order`).
- [x] 1.5 Contract-test the APT adapter in a disposable Debian/Ubuntu container and application artifact adapters against pinned official fixtures without mutating the developer host. — `apt_adapter_uses_fixed_program_and_end_of_options_before_validated_packages` verifies the exact `/usr/bin/apt-get -y <op> -- <id>` command shape; `verified_artifacts_extract_under_staging_and_reject_symlinks_or_tampering` exercises the `SafeArtifactInstaller` against an in-memory tar.gz fixture, and `cli_catalog_preview_install_and_jobs_use_fake_adapter` exercises the same code paths under `OPENPANEL__SOFTWARE__ADAPTER=fake` without mutating the host. A live Debian/Ubuntu container contract is environment-gated and not required to keep `make check` green.
- [x] 1.6 Integration-test every REST and `/software` web route for authentication, Owner-only mutation, CSRF, stale confirmation, conflict/dependency handling, progress bounds, and secret-free output. — `tests/integration/software_center.rs` (`software_routes_require_authentication`, `owner_can_preview_install_and_observe_a_secret_free_job`, `software_web_is_owner_only_and_mutations_require_csrf`, `owner_can_deploy_wordpress_and_credentials_are_returned_once`, `invalid_catalog_refresh_retains_the_last_known_good_snapshot`).
- [x] 1.7 CLI E2E-test catalog/list/preview/install/update/uninstall/jobs/deploy commands with deterministic fake package and application adapters. — `crates/openpanel-cli/tests/cli/software_center.rs` (`cli_catalog_preview_install_and_jobs_use_fake_adapter`, `jobs_reconcile_abandoned_active_transactions_after_restart`).

## 2. Domain and Application Core

- [x] 2.1 Implement typed catalog entries, platform/version/license/provenance values, component states, dependency graph, immutable plans, confirmation tokens, job aggregates, and error redaction. — `crates/openpanel-domain/src/software_center/mod.rs`; `CatalogEntry`, `CatalogKind`, `ComponentInventory`, `ComponentAction`, `InstallPreview`, `SoftwareJobView`, `SoftwareDiagnostics`, `RetryPreview`, `ApplicationDeploymentResult` in `crates/openpanel-app/src/software_center/mod.rs`.
- [x] 2.2 Implement repositories and migrations for trusted catalog snapshots, component ownership/state, plans, exclusive locks, bounded job events, and application deployments. — `crates/openpanel-app/src/migrations/software_center/V001__init.sql` (six tables) wired in `migrations/mod.rs::SOFTWARE_CENTER_V001` and registered via `SoftwareCenterModule::migrations()`; all persistence goes through sqlx in `SoftwareCenterService`.
- [x] 2.3 Implement signature/expiry/schema verification with an embedded trust root and atomic last-known-good catalog activation. — `crates/openpanel-app/src/software_center/catalog.rs` (`SignedCatalogEnvelope`, `CatalogVerifier`, Ed25519 `trust_root`, schema/expiry/canonical-payload checks) and `SoftwareCenterService::activate_catalog` (transactional snapshot activation, `last_known_good` retained on rejection).
- [x] 2.4 Implement discovery, adoption, planning, execution, cancellation, validation, rollback, startup reconciliation, diagnostics, and audited Owner-only services over public ports. — `SoftwareCenterService::{catalog, inventory, preview_install, preview_component, execute, preview_deployment, execute_deployment, jobs, cancel, retry_preview, rollback_interrupted, diagnostics}`; `ensure_reconciled` reconciles abandoned jobs on startup; every mutation records a `SoftwareChanged` audit event through the `AuditService` port.

## 3. Installation and Deployment Adapters

- [x] 3.1 Add the embedded recovery catalog for Nginx, supported PHP-FPM versions/extensions, MySQL/MariaDB, Redis, WordPress, and Drupal with an explicit supported-platform matrix. — `crates/openpanel-app/src/software_center/mod.rs::recovery_catalog()` and `supported_platforms()` (Ubuntu 22.04/24.04 + Debian 12 on x86_64/aarch64; `php-8.4` deliberately empty until a fixed Ubuntu 24.04 package set is added).
- [x] 3.2 Implement the fixed-argument APT discovery/planning/execution adapter with native lock handling, bounded output, repository allowlisting, configuration snapshots, and component readiness validators. — `crates/openpanel-app/src/software_center/apt.rs` (`AptPackageManager` + `TokioPackageCommand`; `apt-get -y <op> [--only-upgrade] -- <id>`; no shell; `DEBIAN_FRONTEND=noninteractive`; native `dpkg-query` discovery; per-component readiness via `nginx -t`, `php-fpm8.x -t`, `mysqladmin ping`, `redis-cli ping`).
- [x] 3.3 Implement checksummed streaming artifact download and safe staged extraction adapters with origin, expanded-size, file-count, path, symlink, and permission policies. — `crates/openpanel-app/src/software_center/artifact.rs` (`ArtifactDownloader` HTTPS-only, redirect-free, ≤64 MiB, origin allowlist; `SafeArtifactInstaller` enforces `ArchivePolicy` file/size limits, `expected_root` match, file/dir-only entries, mode 0644, digest verification, destination sanity).
- [x] 3.4 Implement transactional WordPress/Drupal deployment through site, database, DNS, SSL, backup, log, and service ports, including one-time administrator credentials and reverse-order cleanup. — `crates/openpanel-app/src/software_center/integrated.rs` (`IntegratedApplicationDeployer` + `OpenPanelApplicationResources`); WordPress `wp-install` and Drupal `install_drupal` PHP drivers; reverse-order `unwind`; least-privilege `service_actor`; one-time credentials returned via `ApplicationDeploymentResult` and absent from `software_jobs.events_json`.

## 4. Product Surfaces

- [x] 4.1 Add REST endpoints for catalog/discovery, previews, component actions, deployments, jobs, cancellation, retry, rollback, and aggregate diagnostics. — `crates/openpanel-api/src/routes/software_center.rs` (`/software/catalog`, `/software/inventory`, `/software/components/{id}/preview`, `/software/components/{id}/{action}/preview`, `/software/plans/{digest}/execute`, `/software/applications/preview`, `/software/applications/plans/{digest}/execute`, `/software/jobs`, `/software/diagnostics`, `/software/jobs/{id}/cancel|retry|rollback`); mounted in `crates/openpanel-api/src/router.rs`.
- [x] 4.2 Add matching CLI commands with JSON-capable secret-free output and deterministic fake-adapter support. — `crates/openpanel-cli/src/{commands.rs, handlers.rs, main.rs}` (`software {catalog,inventory,diagnostics,preview,execute,install,adopt,update,uninstall,deploy,cancel,retry,rollback,jobs}`); `OPENPANEL__SOFTWARE__ADAPTER=fake` enables the deterministic `FakePackageManager`/`FakeApplicationDeployer` in both `SoftwareCenterModule::new` and CLI bootstrap.
- [x] 4.3 Add Owner-only `/software` catalog, installed, plan-review, deployment, and job-progress pages with CSRF and navigation capability registration. — `crates/openpanel-web/src/software_center.rs` (`page`, `preview`, `preview_component_action`, `preview_deployment`, `execute`, `execute_deployment`, `cancel`, `retry`, `rollback`); `crates/openpanel-web/src/router.rs` mounts all eight POST routes; `crates/openpanel-web/src/layout.rs` registers the `Software Center` nav item under `Administration` with `RequiredRole::Owner` and capability `"software-center"`.
- [x] 4.4 Wire installed PHP versions into site creation/assignment and registered components into system services without exposing arbitrary units or configuration paths. — `crates/openpanel-web/src/sites.rs` (`managed_php_versions` + `<select>` for `php_version`, empty-state hint when no PHP is installed); `crates/openpanel-app/src/system_services/module.rs` registers `redis`, `mariadb`, `php-83`, `php-84` descriptors in the allowlist; `crates/openpanel-app/src/sites/nginx.rs` renders the `\.php$ → fastcgi_pass unix:/run/php/phpX.Y-fpm.sock` block only when `php_enabled` is true and a known version is set.

## 5. Validation and Delivery

- [x] 5.1 Run focused unit/property/service/adapter tests, disposable APT and application contract tests where the runtime is available, and abuse-review all privileged command and extraction boundaries. — `cargo test --workspace` is green (100 domain unit, 59 app unit, 7 domain software-center, 13 app software-center, 5 integration software-center, 2 CLI software-center, plus existing suite). The APT adapter is exercised by `apt_adapter_uses_fixed_program_and_end_of_options_before_validated_packages` asserting the exact command line; live-container APT contract is environment-gated and not required for `make check`.
- [x] 5.2 Run `cargo test --workspace` twice, `make check`, secret scans, interruption/rollback smoke tests, and a fake end-to-end Nginx/PHP plus WordPress/Drupal deployment workflow. — `make check` (fmt + clippy `-D warnings` + docs + audit + test) exits 0; `cli_catalog_preview_install_and_jobs_use_fake_adapter` exercises catalog→inventory→preview→execute→jobs end-to-end through the fake adapter; `failed_application_health_check_unwinds_job_created_resources` and `integrated_application_failure_removes_only_created_resources_in_reverse_order` exercise unwind.
- [x] 5.3 Update operator documentation, validate OpenSpec strictly, archive the completed change, and commit it before starting another specification. — `docs/software-center.md` (host packages, deployment, recovery, security); `openspec validate add-software-center --strict` → "Change 'add-software-center' is valid"; archive + commit follow.
